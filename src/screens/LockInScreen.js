import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function LockInScreen() {
  return (
    <PlaceholderScreen
      title="Lock-in"
      description="Placeholder only for Phase 1. Lock-in mode is explicitly deferred."
    />
  );
}
